# Wymuszaj LF dla wszystkich plików tekstowych (zapobiega problemom z CRLF na Windows)
* text=auto eol=lf

# Skrypty bash muszą mieć LF - nigdy CRLF (inaczej: "\r: command not found")
*.sh text eol=lf
